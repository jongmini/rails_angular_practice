angular.module('myApp.controllers',[])
.controller('HomeController', function($scope){
});

// Note that, since we’re using the ngmin pre-minifier, we do not need to inject the dependen- cies into our app. (pg 33)